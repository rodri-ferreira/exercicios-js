// Escreva um programa que determina se um número é par ou impar

const verificaParOuImpar = (num) => {
    if(num % 2 === 0) {
        console.log(`${num} é par`)
    } else {
        console.log(`${num} é impar`)
    }
}


verificaParOuImpar(7)
